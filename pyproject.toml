[project]
name = "naija-job-alerts"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "python-telegram-bot==21.4",
    "feedparser==6.0.11",
]

[tool.vercel]
entrypoint = "api.webhook:handler"
